label=joint_GW170104_GW17014
outdir=outdir_joint
accounting = ligo.dev.o3.cbc.lensing.multi

n-triggers = 2
trigger-ini-files = [analysis_GW170104.ini, analysis_GW170104.ini]
verbose = True

sampler = dynesty
n_parallel = 4

common-parameters = [chirp_mass, mass_ratio, mass_1, mass_2, a_1, a_2, tilt_1, tilt_2, phi_12, phi_jl, theta_jn, luminosity_distance, ra, dec]
lensing-prior-dict = {
relative_magnification^(1) = 1,
image_type^(1) = hanabi.lensing.prior.DiscreteUniform(name='image_type^(1)', minimum=1, N=3),
relative_magnification^(2) = Uniform(name='relative_magnification^(1)', minimum=0, maximum=10),
image_type^(2) = hanabi.lensing.prior.DiscreteUniform(name='image_type^(2)', minimum=1, N=3)
}
